# River Crossing Puzzle

This is a simple River Crossing Puzzle built as a static web app using vanilla HTML, CSS, and JavaScript — no frameworks, no build step, just open `index.html`. By choosing a straightforward project, we can focus on learning the AI-DLC workflow rather than the technical complexity of the app itself.
